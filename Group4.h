#ifndef _GROUP4_H_
#define _GROUP4_H_

	

class CGroup4
{
public:
	static long m_Color_Select;
	static COLORREF SelectColor (int CameraX, int CameraY, COLORREF Pix);
	static long GetNumColors();
	

	static void Draw0(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw1(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw2(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw3(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw4(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw5(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw6(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw7(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw8(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw9(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw10(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw11(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw12(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw13(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw14(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw15(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw16(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw17(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw18(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw19(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw20(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw21(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw22(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw23(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw24(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw25(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw26(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw27(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw28(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw29(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw30(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw31(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw32(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw33(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw34(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw35(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw36(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw37(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw38(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw39(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw40(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw41(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw42(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw43(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw44(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw45(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw46(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw47(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw48(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw49(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw50(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw51(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw52(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw53(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw54(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw55(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw56(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw57(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw58(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw59(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw60(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw61(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw62(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw63(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw64(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw65(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw66(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw67(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void Draw68(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);





	static void DrawB01(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawB02(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawB03(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawB04(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawB05(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawB06(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawB07(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawC01(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawC02(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawC03(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawC04(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawC05(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawC06(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawC07(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawC08(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawC09(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawD01(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);


	static void DrawD02(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);


	static void DrawD03(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);


	static void DrawD04(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawD05(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawD06(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawD07(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawD08(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawD09(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawD10(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawD11(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawE01(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawE02(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawE03(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawE04(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawE05(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawE06(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawE07(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawE08(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawE09(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawF01(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawF02(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawG01(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);

	static void DrawG02(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);













	static void DrawBackground(CDC* pDC, CSize& ScreenSize,
		int CameraX, int CameraY, // position on camera
		long x, long y, // position on screen
		long lFactorX, long lFactorY, long CameraFactor,
		COLORREF Pix, COLORREF PrevPix, DWORD bRender);


	static void DrawBackground1();

};



#endif // _GROUP4_H_
